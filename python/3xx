#!/usr/bin/python

import sys
import urllib2

response = None
try:
	response = urllib2.urlopen( sys.argv[1] )
except urllib2.URLError as e:
	print( e.reason )
	if e.reason.index( 'unknown url type: https' ):
		print( "HTTPS error" )
		exit( 255 )
	exit( 2 )

print response
